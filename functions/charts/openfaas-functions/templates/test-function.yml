apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-function
  namespace: openfaas-fn-test
  labels:
    app: test-function
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-function
  template:
    metadata:
      labels:
        app: test-function
    spec:
      containers:
      - name: test-function
        image: 112172658395.dkr.ecr.eu-west-1.amazonaws.com/faas-function-repository:test-function
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: test-function
  namespace: openfaas-fn-test
spec:
  selector:
    app: test-function
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP