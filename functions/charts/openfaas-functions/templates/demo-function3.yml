apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-function3
  namespace: openfaas-fn
  labels:
    app: demo-function3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-function3
  template:
    metadata:
      labels:
        app: demo-function3
    spec:
      containers:
      - name: demo-function3
        image: 112172658395.dkr.ecr.eu-west-1.amazonaws.com/faas-function-repository:demo-function3-v1
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: demo-function3
  namespace: openfaas-fn
spec:
  selector:
    app: demo-function3
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP