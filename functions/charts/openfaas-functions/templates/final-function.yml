apiVersion: apps/v1
kind: Deployment
metadata:
  name: final-function
  namespace: openfaas-fn-test
  labels:
    app: final-function
spec:
  replicas: 1
  selector:
    matchLabels:
      app: final-function
  template:
    metadata:
      labels:
        app: final-function
    spec:
      containers:
      - name: final-function
        image: 112172658395.dkr.ecr.eu-west-1.amazonaws.com/faas-function-repository:final-function
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: final-function
  namespace: openfaas-fn-test
spec:
  selector:
    app: final-function
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP